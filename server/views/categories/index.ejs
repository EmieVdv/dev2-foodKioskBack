<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Ingredients - Backend Food Kiosk</title>
    <link href="/scss/style.css" rel="stylesheet" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <main class="container mt-5">
      <h1 class="text-center mb-4">Categories</h1>

      <!-- Add New Category Button -->
      <div class="text-end mb-3">
        <a href="/categories/add" class="btn btn-success">+ Add New Category</a>
      </div>

      <!-- Category Table -->
      <div class="card">
        <div class="card-header">
          <h2 class="mb-0">All Categories</h2>
        </div>
        <table class="table table-striped">
          <thead>
            <tr>
              <th>Name</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% if (categories && categories.length > 0) { %>
              <% categories.forEach((category) => { %>
                <tr>
                  <td><%= category.name %></td>
                  <td>
                    <a href="/categories/<%= category.category_id %>/edit" class="btn btn-primary btn-sm">Edit</a>
                    <form action="/categories/<%= category.category_id %>/delete" method="POST" class="d-inline">
                      <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            <% } else { %>
              <tr>
                <td colspan="4" class="text-center">No categories found.</td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
